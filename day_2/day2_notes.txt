
DAY 2 Python Training

1. Learn to debug using PDB

    * All exceptions end with ERROR word and has First letter capital, Eg: IndexError

String Programs:
    1. UpperCase: variable.upper()
    2. LowerCase: variable.lower()

git status: tells status

git add . // states changes

git commit origin main // commit to origin 
git commit -m 'note' // creates obejsct with all files and folders

git pull origin main // sync the git repo into local machine

git push origin main // Syncs the object to the git object